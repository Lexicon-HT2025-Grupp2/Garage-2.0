@model IEnumerable<ParkedVehicleViewModel>

@{
    ViewData["Title"] = "Overview";
}

<h1>Overview</h1>

<p>
    @* <a asp-action="Create">Create New</a> *@
    <div class="d-flex justify-content-between align-items-center mb-3">
        <a asp-action="CheckIn" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Check In Vehicle
        </a>
        <form asp-action="Search" class="col-md-9">
            <div class="input-group">
                <input type="text" class="form-control" name="searchTerm" placeholder="Search..." />
                <button class="btn btn-outline-secondary" type="submit">Search</button>
            </div>
        </form>
    </div>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegistrationNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Color)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumberOfWheels)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArrivalTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Note)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RegistrationNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Color)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfWheels)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArrivalTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @* <a asp-action="Edit" asp-route-id="@item.RegistrationNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RegistrationNumber">Details</a> |
                <a asp-action="CheckOut" asp-route-id="@item.RegistrationNumber">Check out</a> *@
                    <a asp-action="Details" asp-route-id="@item.RegistrationNumber" class="btn btn-sm btn-info" title="View Details">
                    <i class="bi bi-eye"></i>
                </a>
                    <a asp-action="Edit" asp-route-id="@item.RegistrationNumber" class="btn btn-sm btn-warning" title="Edit">
                    <i class="bi bi-pencil"></i>
                </a>
                    <a asp-action="CheckOut" asp-route-id="@item.RegistrationNumber" class="btn btn-sm btn-danger" title="Check Out">
                    <i class="bi bi-box-arrow-right"></i>
                </a>
            </td>
        </tr>
}
    </tbody>
</table>
